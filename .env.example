# Supabase — copy to `.env` and fill in from your project's API settings.
# The app runs fully on local storage without these; set them to enable
# cloud auth + sync (see README "Enable Supabase").
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
